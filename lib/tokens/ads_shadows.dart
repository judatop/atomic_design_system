import 'package:flutter/material.dart';

class ADSShadows {
  ADSShadows._();

  static const BoxShadow smallShadow = BoxShadow(
    color: Color.fromRGBO(240, 10, 4, 0.2),
    blurRadius: 10,
    spreadRadius: 2,
    offset: Offset(0, 4),
  );
}
